version: '3.7'

services:
  sample:
    container_name: sample-react-app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - "localhost:8081:8081"
    environment:
      - CHOKIDAR_USEPOLLING=true